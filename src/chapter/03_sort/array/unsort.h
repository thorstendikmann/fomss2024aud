#ifndef UNSORT_H
#define UNSORT_H

#include "intarray/intarray.h"

/**
 * Unsort a given array, shuffle and randomize it.
 * @param a the array to be randomized.
 */
void array_unsort(Array *a);

#endif // UNSORT_H